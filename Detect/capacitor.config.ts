import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.facedetect.facedetectapp',
  appName: 'detect-face',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
